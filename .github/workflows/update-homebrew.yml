name: Update Homebrew

on:
  release:
    types: [published]

jobs:
  update-homebrew:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger homebrew-tap update
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.HOMEBREW_TAP_TOKEN }}
          repository: salmonumbrella/homebrew-tap
          event-type: update-formula
          client-payload: |
            {
              "formula_name": "docuseal-cli",
              "version": "${{ github.event.release.tag_name }}",
              "repo": "salmonumbrella/docuseal-cli",
              "binary_name": "docuseal",
              "archive_prefix": "docuseal",
              "description": "CLI for DocuSeal document signing"
            }
